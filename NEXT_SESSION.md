# На чём остановились

Файл читается в начале каждой сессии (хук `SessionStart` печатает первые 40 строк).
Держать коротким: здесь состояние, а не история. История — в журнале карты
(`/home/claude/karta`) и в git.

Секции старше двух недель удалять.

---

## 17.09.2026, вечер

**План на завтра — `PLAN-2026-09-18.md`.** Цель дня: довести разбор до качества,
с которым его можно показать отделу продаж.

**Что сделано вечером.**
- Разбор прозвона за 17.09 шёл всю ночь (144 разговора). Проверить, досчитался ли.
- Распознавание ускорено вдвое: дорожки звонка считаются параллельно
  (`ASR_CHANNEL_PARALLELISM=2`, `ASR_CPU_THREADS=2`, `num_workers` у модели).
  Замер: 0,66 → 0,34 от длительности записи.
- Починена причина падений разбора: сборщик держал одну транзакцию на всё
  листание истории Synergy, теперь коммитит пачками по 100.
- Два таймера: `dashboard-selfcheck` (06:00 МСК) и `dashboard-morning` (07:00 МСК).
  Отчёт — `/karta/morning.html` и `data/morning.md`.
- Проба распознавания через API на 30 разговорах: `data/сравнение-распознавания.md`
  в проекте ASR. Итог: годится только `whisper-1`, у новых моделей нет границ фраз.

**Открыто за владельцем.**
- 12 находок ждут подтверждения на `/dashboard/leads`.
- Решение по распознаванию: местная модель или `whisper-1` (~$216/мес, и это
  ещё и заметно точнее). Рекомендация — `whisper-1` на прозвон, просев оставить
  местной модели.
- Боевой сервер в России открыт наружу: вход по паролю и под root, файрвол
  выключен, 5700 подборов за сутки. Вечером поставлен fail2ban. Остальное
  чинить только вместе и со второй открытой сессией.
- Резервных копий базы и записей нет вовсе.

**Что дальше по коду.**
- Таймера на разбор прозвона по-прежнему нет — запускается руками.
- Неразобранный хвост за прошлые дни: 428 разговоров.
- Разговор 8229011 роняет просев с `415 Unsupported Media Type`.
